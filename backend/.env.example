# OpenAI API Key (optional - falls back to pattern matching if not set)
# Get your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Server port (optional, defaults to 8081)
PORT=8081
